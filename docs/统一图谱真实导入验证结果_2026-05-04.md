# 统一图谱真实导入验证结果（2026-05-04）

## 1. 本次验证结论
统一图谱已经完成一次真实导入验证，结果分两部分：

### 结构层
验证通过：
- IndustryDomain 已导入
- SubTrack 已导入
- ChainStage 已导入
- KeyCapability 已导入
- ApplicationScenario 已导入
- HAS_STAGE / UPSTREAM_OF / REQUIRES_CAPABILITY / DRIVES_STAGE 已导入

### 企业挂接层
首次验证未通过，原因是：
- 统一关系表使用的是 `E001` 这套企业 ID
- 本地现有 Neo4j Enterprise 节点主键仍是旧的 `ENT_xxx`

已修正方式：
- 新增 `unified_enterprise_id_mapping.csv`
- 生成 `*_legacyid.csv` 版企业挂接关系表
- 使用 `import_unified_enterprise_patch_legacyid.cypher` 重新补导企业挂接

修正后验证通过。

---

## 2. 结构层验证结果
### 节点数量
- IndustryDomain：1
- SubTrack：3
- ChainStage：46
- KeyCapability：18
- ApplicationScenario：16

### 关系数量
- HAS_SUB_TRACK：3
- HAS_STAGE：46
- UPSTREAM_OF：43
- REQUIRES_CAPABILITY：36
- DRIVES_STAGE：16

说明：
- `REQUIRES_CAPABILITY` 实际导入结果为 36，较原预估 35 多 1 条，属于正常结果，以数据库实际导入为准。

---

## 3. 企业挂接层验证结果
### 按子赛道统计企业数量
- 光通信 / 光模块：5
- 光学检测 / 机器视觉：4
- 激光加工 / 光学装备：1

说明：
- 该结果与当前纳入的 10 家样板企业一致

### 样例企业验证
验证企业：`中科慧远视觉技术（洛阳）有限公司`

验证结果：
- 子赛道：光学检测 / 机器视觉
- 环节：专用检测设备
- 关键能力：
  - 成像方案设计能力
  - 高精度检测算法能力
  - 行业方案交付能力

说明：
- 统一图谱中的“企业 -> 子赛道 -> 环节 -> 关键能力”主路径已打通

---

## 4. 本次新增/使用的关键文件
### 导入脚本
- `neo4j/import_unified_v1.cypher`
- `neo4j/import_unified_enterprise_patch_legacyid.cypher`

### 验证脚本
- `neo4j/verify_unified_v1.cypher`

### ID 映射与补丁关系表
- `data/mappings/unified_enterprise_id_mapping.csv`
- `data/mappings/unified_rel_enterprise_to_subtrack_legacyid.csv`
- `data/mappings/unified_rel_enterprise_to_stage_legacyid.csv`
- `data/mappings/unified_rel_enterprise_to_keycapability_legacyid.csv`

---

## 5. 当前判断
统一图谱现在已经不是“文档上可行”，而是已经达到：

> **结构可入库、企业可挂接、查询可跑通。**

也就是说，统一图谱 V1 的最关键闭环已经完成。

---

## 6. 仍待继续的问题
### 1）企业主键体系尚未彻底统一
当前数据库中企业仍是旧 `ENT_xxx` 主键。
后续建议二选一：
- 方案A：继续保留旧主键，统一通过 mapping 层适配
- 方案B：下一轮重构时把 Enterprise 主键整体切换到 `E001` 体系

短期建议：**先保留旧主键 + mapping 适配**，改动最小。

### 2）MAPPED_TO 映射层仍只是首版
当前：
- `Capability -> KeyCapability`
- `Scenario -> ApplicationScenario`

都只是样板链相关对象的首版映射，不是全量清洗结果。

### 3）TechDirection 仍未正式落库
当前统一结构已经可跑，但技术主题层还未正式补进主数据与查询体系。

---

## 7. 下一步建议优先级
1. 把 `legacyid` 方案写入正式导入说明，避免后续重复踩坑
2. 补齐 `Capability/Scenario` 的全量映射层
3. 决定 Enterprise 主键长期方案
4. 再做商业航天第四条样板链预研

---

## 8. 一句话结论

今天这轮已经把统一图谱从“设计完成”推进到了：

**真实导入成功，真实查询跑通。**
