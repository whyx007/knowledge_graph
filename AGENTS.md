# AGENTS.md

## Workspace Convention

- Unless the user explicitly asks for v1, discuss and modify the v2 workspace by default.
- Default target path for future knowledge-graph changes: `v2_pg_finegrain/`.
- Treat `v1_pg_finegrain/` as legacy/baseline material for reference only unless the user names it.
