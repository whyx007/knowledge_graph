# 今日续做进展补充（2026-05-04）

基于 `docs/明日续做清单.md`，本轮已继续推进统一图谱收尾。

## 已完成

### A. 已补齐统一图谱产业结构关系表
新增：
- `data/mappings/unified_rel_subtrack_to_stage.csv`
- `data/mappings/unified_rel_stage_to_stage.csv`
- `data/mappings/unified_rel_stage_to_keycapability.csv`
- `data/mappings/unified_rel_appscenario_to_stage.csv`

说明：
- 已把三条样板链的子赛道-环节、环节上下游、环节-关键能力、应用场景-环节关系补齐为统一导入格式
- 当前统一图谱骨架已基本闭环

### B. 已补一版旧对象到统一对象的映射层（首版）
新增：
- `data/mappings/unified_rel_capability_to_keycapability.csv`
- `data/mappings/unified_rel_scenario_to_appscenario.csv`

说明：
- 当前仅补了**样板链相关、可高置信确认**的首版映射
- 不是全量旧 Capability / Scenario 的完整映射清洗结果
- 适合作为统一查询接线样板，不宜误认为全库已清洗完成

### C. 已生成统一图谱正式导入脚本
新增：
- `neo4j/import_unified_v1.cypher`

说明：
- 脚本覆盖统一主数据节点导入
- 覆盖统一结构关系导入
- 覆盖企业挂接关系导入
- 预留旧图谱映射关系导入位置

### D. 已升级统一图谱查询模板
新增：
- `neo4j/query_templates_unified_graph.cypher`
- `docs/统一图谱查询说明.md`

说明：
- 已支持按子赛道看企业
- 已支持按环节找企业
- 已支持查看企业在产业链中的位置
- 已支持从关键能力反查企业与环节
- 已支持从应用场景反查环节与企业

---

## 当前状态判断

统一图谱现在已经不再只是：
- 有主数据
- 有企业挂接

而是已经进一步具备：
- 产业结构骨架
- 正式导入脚本
- 统一查询模板

也就是说，A / C / D 基本已完成第一版闭环。

---

## 还剩下的缺口

### 1. B 映射层仍未全量完成
主要还缺：
- 对全部旧 Capability 的系统映射清洗
- 对全部旧 Scenario 的系统映射清洗
- 必要时补 `Industry -> MAPPED_TO_SUB_TRACK`

### 2. TechDirection 仍未正式落主数据
当前统一物理模型中有该对象，但尚未进入主数据 CSV 与导入脚本

### 3. 还没实际执行统一导入验证
建议下一步：
- 将新增 CSV 复制到 Neo4j import 目录
- 执行 `schema_unified_v1.cypher`
- 执行 `import_unified_v1.cypher`
- 用统一查询模板做一次演示验证

---

## 建议下一步优先级
1. 实际执行统一导入验证
2. 补齐 B 的全量映射层
3. 补 `TechDirection` 主数据
4. 再决定是否进入商业航天预研

---

## 一句话结论

今天这轮续做，已经把“统一图谱方案”推进到了**可导入、可查询、骨架闭环**的状态；下一步最重要的是做一次真实导入验证，而不是继续扩文档。
